name: Scheduled Workflow

on:
  schedule:
    - cron: "30 21 * * 4"

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Echo current time
        run: echo "The current server time is $(date), time for the meeting"
