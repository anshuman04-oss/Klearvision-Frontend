function ImageDownload() {
    // Using this component, the user will be able to download the processed image.
    // For getting the processed image, we will call API and then convert the image to any format like jpg/pdf/png
    // const [image, setImage] = useState(null)

    

    return (
        <div>
            <div>Download the Image</div>
        </div>
    )
}

export default ImageDownload
